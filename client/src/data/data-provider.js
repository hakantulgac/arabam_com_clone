import data from "./data.json";

export const apiUrl = `${process.env.REACT_APP_API_URL}`;
export const {
    headerDropItems, 
    footerMenuItems, 
    footerBrands,
    footerLeftMenuItems,
    footerContactLinks,
    mainMenuItems,
    highlightItems,
    sideMenuItems,
    vehicleTitles,
    coopItems,
    newsComponents,
} = data;

